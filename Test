@ExtendWith(MockitoExtension.class)
class AlertServiceTest {

    @InjectMocks
    private AlertService alertService;

    @Mock
    private AlertDao alertDao;

    @Mock
    private MerchantUserValidator merchantUserValidator;

    @Test
    void testGetAllAlertsByMId_success() {
        // Given
        String mId = "M123456";
        Pageable pageable = PageRequest.of(0, 10);
        List<AlertManagementResponse> alertList = List.of(
                AlertManagementResponse.builder().alertTitle("Title1").build(),
                AlertManagementResponse.builder().alertTitle("Title2").build()
        );
        Page<AlertManagementResponse> alertPage = new PageImpl<>(alertList, pageable, alertList.size());

        when(alertDao
